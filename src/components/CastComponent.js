import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CastComponent = () => {
  return (
    <View>
      <Text>CastComponent</Text>
    </View>
  );
};

export default CastComponent;

const styles = StyleSheet.create({});
